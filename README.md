# Project Layout

- `Task-1/`: WikiArt multi-task training, prediction, outlier review notebooks, and audit data.
- `Task-2/`: separate Task 2 work.

Run Task 1 commands from `Task-1/`, or prefix paths from the repo root with `Task-1/`.
